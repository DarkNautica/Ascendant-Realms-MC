# Loot Table Control

Status: debug Atlas waymark loot tables retained as source material; broader loot rewrite pending.

Atlas debug waymarks use low-tier travel loot only if placed manually. Live structure and dungeon loot should follow `config/ascendant_core/loot_policy.json`, `config/ascendant_core/structure_rewards.json`, and the generated rarity registry.
