# Known Risks

- Loader split: 1.20.1 Forge supports many classic Forge mods, but the modern RPG Series stack favors 1.21.1 NeoForge/Fabric.
- Worldgen overload: Terralith, Tectonic, multiple village mods, YUNG suite, Structory, and Moog structures can create structure spam or slow generation.
- Ascendant Atlas is now active as a finite-world coordinate runtime, In Control area layer, policy files, debug/status datapack, and local `ascendant_atlas_regions` biome-source helper. Coordinate-locked north/south/east/west biome identity is implemented for new worlds, but hard regional ore rules, terrain-aware road-to-bridge substitution, and structure-pool conflict resolution still require later helper work or a curated map workflow.
- Boss/mob overload: Ice and Fire, Born in Chaos, Alex's Mobs, Mowzie's Mobs, Aquamirae, and Enhanced Celestials need spawn tuning.
- Power creep: Simply Swords, Artifacts, spellbooks, boss loot, and bounty rewards can overpower players too early.
- Shader performance: Oculus/Iris plus heavy particles/animations can hurt FPS.
- Client/server split: shader loaders, resource packs, animations, and UI mods should not go to the server unless explicitly required.
- Traveler's Titles visual resource packs are client-only. Visual Traveler's Titles and Visual Traveler's Title Biomes Addon are installed for 1.20.1; Biome Edition Visual Traveler's Titles is delayed because verified files target 1.21.1. The local Ascendant Realms fallback gives readable Terralith/IceAndFire/Iron's titles, but it is not handmade logo art.
- Dynamic Trees compatibility: beautiful but expensive to integrate with biome/worldgen stacks.
- Sinytra Connector: not allowed in main path.
- OptiFine: not allowed.
- Batch A export risk: CurseForge client export may bundle Modrinth-sourced jars for private local testing. Do not distribute exported ZIPs without checking licenses.
- JEI is intentionally client-only for this proof. If a later server-side recipe integration needs it, revisit side metadata deliberately.
- Traveler's Titles originally pulled YUNG's API as a client-side dependency; Batch B now uses YUNG structures, so YUNG's API is both-side.
- Subtle Effects 1.20.1-1.14.3 requires Forge 47.4.14 or newer. The pack target is now Forge 47.4.20 because Forge 47.4.10 caused the Batch A startup crash.
- Fzzy Config requires Kotlin for Forge in the 4.x line, specifically >=4.11.0 and <=4.99.0 for the current file. Kotlin for Forge 4.12.0 is installed; do not upgrade it blindly to a newer incompatible major version.
- Batch A passed client, shader, dedicated server, multiplayer join, and 10-minute stability validation.
- Subtle Effects must remain both-side while active. Keeping it client-only crashed dedicated multiplayer with `Registry Object not present: subtle_effects:waterfall_droplet`; Fzzy Config and Kotlin for Forge follow it server-side.
- Particular Reforged must be treated as both-side for now because the Forge multiplayer handshake required it during validation.
- Batch B worldgen passed fresh-world dedicated server validation with terrain generation, structure generation, disconnect/rejoin, and 10-minute stability.
- CurseForge may create duplicate instance folders such as `Ascendant Realms (1)` or `Ascendant Realms (2)`. The materializer must point to the active instance containing the current batch jars. Current active path for the latest sync/export pass: `C:\Users\Jayden\curseforge\minecraft\Instances\Ascendant Realms (2)\mods`.
- Sparse Structures and the four intended YUNG structure mods are marked both-side for private/local testing so CurseForge client import places their jars in the active instance. This resolved the earlier missing-jar materialization issue.
- Tectonic's selected file name includes `1.19.3`, but Modrinth metadata marks the selected version as compatible with Minecraft 1.20.1. If worldgen errors appear, verify Tectonic first.
- Batch C combat foundation passed client and dedicated server validation.
- Better Combat, Combat Roll, Simply Swords, playerAnimator, Cloth Config API, and Architectury API are both-side and stable enough for the next batch.
- Simply Swords can still introduce power creep if high-tier weapons are easy to obtain. Keep loot/progression review active before a committed survival world.
- Batch D skills/classes/stat foundation passed solo and dedicated server validation.
- Pufferfish's Attributes and Pufferfish's Skills are both-side for the current multiplayer-safe workflow.
- Default Skill Trees was removed from active Packwiz metadata after the custom Ascendant Realms skill tree was approved. Do not re-add it unless the custom tree is deliberately rolled back.
- The first custom skill-tree test reported no skill tree found because the tree existed only as a world datapack source. The active load path is now `config/puffish_skills/`, which Puffish Skills can load globally for new worlds.
- Skill node tooltips are styled in Puffish `definitions.json`, not by Legendary Tooltips. Legendary Tooltips affects item tooltips; custom skill nodes must keep their visible `Effect:` line and Shift metadata inside the Puffish skill definition files.
- The custom tree uses direct attribute rewards where attributes were documented or detected. The first unique-identity pass now uses Iron's Spells max mana, mana regeneration, cooldown reduction, cast time reduction, spell resistance, and spell-school attributes, plus Pufferfish specialty attributes such as stealth, tamed damage/resistance, fortune, shred, damage reflection, fall reduction, projectile speed, mount speed, and sustain. True boss/dragon gates, Bountiful rewards, spell-cast event XP, and Combat Roll-specific cooldown/stamina hooks still need later datapack/advancement/config/KubeJS work.
- The richer custom skill tree must be revalidated in client and dedicated server before treating Batch I as passed. Watch especially for unknown attribute IDs, overpowered Iron's Spells mana/cooldown scaling, tamed dragon/companion scaling, and shred/reflection stacking against bosses.
- Short survival feedback: the world is beautiful but too empty, not dangerous enough, and light on scary mobs and structures.
- Batch E1 world pressure and density passed validation.
- Batch validation policy no longer requires disposable survival throwaway tests after every batch. Use client boot, creative/system functionality, dedicated server boot/join, and a 10-minute stability check.
- Batch E2 loot, rewards, and contracts passed client creative/system testing, dedicated server boot/join, and 10-minute stability. Real survival tuning is now active; final balance is not complete.
- In Control starter rules include conservative caps plus Atlas coordinate-area guardrails. They still do not add custom spawns, stat boosts, distance scaling, loot changes, magic, or boss progression.
- Sparse Structures remains installed with local config. The current density repair uses global `1.25`, vanilla village factor `1.0`, and pillager outpost factor `1.1`.
- Mowzie's Mobs and Alex's Mobs can create danger spikes. Validate balance during later survival tuning before committing a real world.
- MVS and YUNG's Extras can improve discovery, but they may also interact with Sparse Structures or create density/performance issues during longer exploration.
- Enhanced Boss Bars is client-only unless multiplayer testing proves otherwise.
- Artifacts and Bountiful are now installed in Batch E2. Watch for early power creep from artifacts, bounty rewards, and loot-table interactions before a committed survival world.
- Artifacts, Bountiful, Villager Names, Loot Integrations, Kambrik, Collective, and Fragmentum are stable enough for the next batch after E2 validation.
- First E2 client load failed because Artifacts was missing Curios API and Loot Integrations was missing Cupboard. Curios API `5.14.1+1.20.1` and Cupboard `1.20.1-3.7` are now installed and must remain both-side while Artifacts and Loot Integrations are active.
- Loot Beams and Loot Journal are client-only. Do not copy them to the server unless multiplayer testing proves a handshake requirement.
- Loot Beams' native distance cap is `24.0`, and its built-in rarity filter is non-common rather than true Epic-only. The current config disables all-item beams and combines name plus rarity into one look-at-item tooltip. The previous loot-beam/UI concern is not a Batch E2 blocker; full balance/UI tuning happens later after the major stack is installed.
- Loot Integrations is installed as a server-side loot bridge. YUNG Structures Addon for Loot Integrations is delayed because the visible 1.20.1-specific recent file was Fabric, not a clean Forge 1.20.1 target.
- Batch F Arcane Nightfall Expansion passed client creative/system testing and dedicated server boot/join plus 10-minute stability. It is intentionally larger than prior batches and adds magic, enemy escalation, dangerous events, ocean/ice danger, gear, travel/exploration tools, and medieval build polish.
- Batch F installed Iron's Spells, Born in Chaos, Aquamirae, Enhanced Celestials, Bosses'Rise, Immersive Armors, Spartan Shields, Small Ships, Snow! Real Magic, Handcrafted, Macaw's Bridges, and Macaw's Fences and Walls.
- Batch F dependencies added by Packwiz: Iron's Lib, Obscure API, CorgiLib, Data Anchor, Kiwi, and Resourceful Lib. Curios API, GeckoLib, and Citadel were already present.
- First Batch F client load failed because Iron's Spells was missing Iron's Lib and Aquamirae was missing Obscure API. Iron's Lib `1.20.1-1.0.2` and Obscure API `18` are now installed and must remain both-side while Iron's Spells and Aquamirae are active.
- Iron's Spells, Born in Chaos, Aquamirae, Enhanced Celestials, Bosses'Rise, Immersive Armors, Spartan Shields, Small Ships, Snow! Real Magic, Handcrafted, Macaw's Bridges, and Macaw's Fences/Walls are stable enough for the next batch.
- Batch F danger pressure can still spike. Full balance tuning happens later; review Born in Chaos, Aquamirae, Enhanced Celestials, and Bosses'Rise configs during the tuning pass if the world feels chaotic.
- Variants & Ventures is delayed/removed from the active pack after the Batch J startup crash. Crash signature: `Rendering overlay`, `No model for layer variantsandventures:murk_skull#main`, involving Variants & Ventures and Entity Model Features. Do not restore the 1.20.1 Forge file unless a clean fix appears or EMF strategy changes.
- Batch G Dragonforge Cataclysm passed client creative/system testing and dedicated server boot/join plus 10-minute stability. It adds IceAndFire Community Edition, L_Ender's Cataclysm, Marium's Soulslike Weaponry, Create, Create Big Cannons, Farmer's Delight, and Create: Structures Arise.
- Batch G dependencies added by Packwiz: Jupiter, Uranus, Lionfish API, AttributeFix, Projectile Damage Attribute, and Ritchie's Projectile Library. Curios API and GeckoLib remain both-side from earlier batches.
- First Batch G client load failed because Marium's Soulslike Weaponry rejected Projectile Damage Attribute `3.2.3+1.20.1`. Projectile Damage Attribute is now pinned to Modrinth file `projectile_damage-forge-3.2.2+1.20.1.jar` and must stay below the rejected `3.2.3` file unless a later test proves a newer file is safe.
- IceAndFire Community Edition was selected over original Ice and Fire because it has a current Minecraft 1.20.1 Forge/NeoForge file. It is a community fork and is not a direct replacement path for existing original Ice and Fire saves.
- Original Ice and Fire is delayed so exactly one Ice and Fire variant is active. Do not swap variants after a real survival world starts without a separate migration plan.
- Batch G can spike danger, structure density, loot power, food sustain, and server load. Use `docs/DRAGON_AND_BOSS_TUNING.md` for later tuning targets.
- Batch H Civilization and Atmosphere is installed and verified. It adds Villages&Pillages, MSS, MES, Auroras, Beautiful Enchanted Books, Perception, OctoLib/ShatterLib, and Medieval Buildings End/Nether editions.
- CTOV and Lithostitched are delayed after CTOV crashed during `ctov:medium/village_swamp` feature placement with a `Holder$Reference` to `PoiType` class-cast error.
- Batch H can increase village/landmark density and worldgen load. It passed the batch validation gate, but longer-world structure balance still belongs to later survival tuning.
- Auroras, Beautiful Enchanted Books, Perception, and OctoLib/ShatterLib are client-only for Batch H. Do not copy them to the dedicated server unless multiplayer testing proves a handshake requirement.
- Biome Music and Medieval Music were delayed from Batch H but are now installed in Batch L after clean Packwiz resolution. Neko's Enchanted Books is delayed because Beautiful Enchanted Books was selected.
- Batch J visual/add-on candidate pass is installed and verified. It adds Fantasy Armor, Wavey Capes, Xaero's Minimap, Advancement Plaques, Malfu Combat Animation, and several resource packs.
- T.O Magic is delayed because `traveloptics-6.3.0-1.20.1.jar` crashed on startup against Cataclysm `3.30` with missing `DungeonEyeItem`. Do not downgrade the validated Cataclysm stack just to satisfy this add-on.
- Alex's Caves, Apothic Attributes, and Placebo were removed with T.O Magic because they were only added by that dependency chain.
- Iron's Spells remains updated to `3.16.1` and Iron's Lib to `1.1.0` from the attempted T.O Magic install; those versions passed Batch J validation, but downgrade later if they cause future trouble.
- Malfu Combat Animation can conflict visually or mechanically with Better Combat, Simply Swords, playerAnimator, Wavey Capes, and shader/animation settings.
- Wavey Capes, Xaero's Minimap, Advancement Plaques, and all Batch J resource packs are client-only. Do not copy them to the server unless a specific handshake error proves otherwise.
- STONEBORN, Excalibur, Vanilla Experience+, Cubic Leaves, Cubic Sun & Moon, Icon Xaero's, Icon Fresh, Medieval Music, and The Rename Compat Project can conflict through resource-pack ordering. Resource Pack Overrides now marks the intended external resource packs required/force-compatible; if a pack still disables itself after restart, first confirm the active CurseForge import includes `ResourcePackOverrides-v8.0.3-1.20.1-Forge.jar`, then inspect `config/resourcepackoverrides.json` and root `options.txt` before changing gameplay mods.
- Resource-pack reset root cause: the active log showed `Failed to load options` through Minecraft's old keybind migration path (`OptionsKeyLwjgl3Fix`) after encountering `key.keyboard.left.alt`. Keep root and active `options.txt` at `version:3465`, write it as UTF-8 without BOM, and install Resource Pack Overrides into the active client before chasing resource-pack order.
- TravelersCrossroads is delayed because Packwiz refused it for Minecraft 1.20.1 Forge and the visible current provider files target 1.21.x NeoForge.
- Batch K is installed and verified. The active set is Titles, Health Bar Plus, Scaling Health, Silent Lib, Sound Physics Remastered, Weather, Storms & Tornadoes, and CoroUtil.
- Weather, Storms & Tornadoes has the cleanest 1.20.1 Forge weather path for Batch K but can create griefing, storm-frequency, visual-noise, and TPS risk. Start conservative and reduce/disable destructive effects if the config exposes those controls.
- Scaling Health can over-buff early game and stack with custom skills, Artifacts, Iron's Spells, Simply Swords, and boss loot. Start with conservative scaling and rare blights only.
- Health Bar Plus can clutter the screen if passive, neutral, or hostile mobs are always visible. The pack now carries `config/healthbarplus-client.toml` plus an Ascendant Nametags bridge so bars show for the mob under the crosshair or recent combat targets.
- The zero-audio report happened while Minecraft volumes were at 100% and the log showed OpenAL using `SteelSeries Sonar - Gaming`. It was traced to headphone/output routing, so Sound Physics Remastered is restored. If silence returns, inspect Minecraft's output device and Windows/Sonar routing before changing mods.
- Simple Clouds / Project Atmosphere is delayed for Batch K. Simple Clouds is open beta, Project Atmosphere's current 1.20.1 Forge metadata depends on Simple Clouds and Gabou's Libs, and a recent public report shows Simple Clouds plus Project Atmosphere crashing together.
- IntegratedPlaytime is optional only as a secondary tracker. Main Ascendant rank/title progression should come from skills, boss kills, dragon kills, contracts, and milestones, not playtime.
- Batch L is installed and verified. The active server-side pressure set is Spawn Balance Utility, Majrusz's Progressive Difficulty, Majrusz Library, Improved Mobs, and TenshiLib.
- Batch L can stack danger through Scaling Health, Majrusz's Progressive Difficulty, Improved Mobs, Born in Chaos, IceAndFire CE, Cataclysm, and Marium. Keep the validation gate focused on boot, creative/system function, server join, and 10-minute stability; tune survival balance later.
- Batch L client-only presentation/audio mods are Not Enough Animations, AmbientSounds 6, CreativeCore, Presence Footsteps, Biome Music, Medieval Music, and Sound Physics Remastered. Do not copy them to the dedicated server unless a test proves they are required.
- First-person Model is removed because Jayden disliked the first-person body view.
- AmbientSounds `v6.1.0` crashed on startup with CreativeCore `2.12.38` through a missing `ConfigEventHandler.load(String, Side)` method. AmbientSounds is now pinned to `v6.3.8`; stale CurseForge instances with the old jar must be cleaned or reimported.
- Better Animations Collection is delayed because it overlaps with Fresh Animations, EMF, ETF, and the current animation/resource-pack stack.
- Batch N is installed and verified. The active set is KubeJS, Rhino, Open Loader, Almost Unified, Almost Unify Everything, Polymorph, Every Compat, Moonlight Lib, Create Slice & Dice, Alex's Delight, Integrated Villages, IDAS, Integrated API, Supplementaries, Quark, and Zeta.
- Crash fix diagnosis: Integrated Villages has reproduced the same `Holder$Reference` to `PoiType` class-cast worldgen crash from `integrated_villages:airship_village`, `integrated_villages:mossy_mounds`, and now `integrated_villages:marketstead_village`. The shared root path is `integrated_api:workstation_processor`. The active OpenLoader fix overrides Integrated Villages processor lists with static `minecraft:rule` workstation placeholder replacements, keeps the repaired structures enabled for retest, and repairs Integrated Villages' broken `minecraft:village` tag by removing only nonexistent `integrated_villages:swamp_village`. Latest client crash seed was `3740828705519225665`, generated chunk `-33,-26`, crash UUID `7d22f69c-7353-4114-aeaa-0681225fd933`; keep older retest seeds `4571938849163387743` and `-8696758597753506463` as secondary coverage.
- The world-integration audit is an inventory, not a balance pass. It found 623 structures, 333 structure sets, 1,586 template pools, 696 placed features, 631 configured features, 98 biome modifiers, 614 biome tags, 4,324 loot tables, 10,551 recipes, 1,065 item tags, and 145 entity type tags across the latest active jar set. Final tuning still needs in-game validation.
- KubeJS recipe/tag/loot scaffolds are intentionally non-invasive, but the progression HUD and Ascendant Core loader are active. If KubeJS errors appear during boot, inspect the exact script path and syntax before assuming the gameplay stack is broken.
- The active skill tree loads from `config/puffish_skills/`. The repo-level OpenLoader skill mirror is retained as source/legacy material only; do not move it into the active OpenLoader path unless a later test explicitly needs global datapack loading and checks for duplicate Puffish definitions.
- Almost Unified and Almost Unify Everything must not collapse unique dragon, boss, spell, Artifact, Create mechanism, Bountiful, or skill-tree milestone items into common commodities.
- Generated CustomNPCs now receive MCA-medieval-style bridge skins and visible equipment from `npc_loadouts.json`. This should be preservative because it changes only generated NPC entity NBT and resource lookup paths, not item stats or player loot tables; the full visible kit uses `0.0` drop chances. CustomNPCs visible gear is written to top-level `Weapons`/`Armor`, while `NpcInv` stays empty because it is the drop-table inventory. First in-game skin test showed magenta missing textures from `customnpcs:textures/entity/ascendant_mca/*.png`; the repair mirrors the same valid PNGs into CustomNPCs' own `customnpcs/assets/` resource surface in addition to the compatibility resource pack. Retest that CustomNPCs honors the bridged textures and visible gear in-game before treating the generated NPC layer as passed.
- Generated NPC AI is improved but not final. Social NPCs are anchored to avoid synchronized wandering, and combat profiles have wider active/walking ranges. If guards/rivals still ignore modded hostiles, build a CustomNPC faction/script layer or a small Ascendant NPC Runtime helper mod rather than removing NPC functionality.
- Standalone Ascendant Guild structure density was reduced after the first pass produced too many board structures near a village. Current spacing is Hunter Board `192/80`, Roadside Camp `224/96`, and Frontier Outpost `288/128`. If villages feel sparse later, tune spacing gradually rather than adding another village/structure pack.
- Standalone Ascendant Atlas waymarks are debug-only assets and should not naturally generate. If they appear in fresh worldgen, the structure-set files were accidentally restored and should be removed again; do not remove the Atlas runtime/control layer.
- Supplementaries notice boards are active social presentation blocks. They now use multiple written boards per structure and are generated facing outward; one overloaded board is not readable enough for Guild rankings.
- Integrated Villages is active because villages still felt bland after CTOV was removed, but it overlaps with Towns and Towers, Villages & Pillages, MVS, Structory, Bountiful boards, Guard Villagers, and Villager Names.
- IDAS overlaps with YUNG, Moog structures, Cataclysm structures, Marium progression, Artifacts, Loot Integrations, and current structure-density tuning. Treat structure placement crashes as Batch N blockers.
- IDAS pulled Supplementaries and Quark. These add gameplay/building content beyond pure integration, so test menus, tooltips, JEI, and worldgen before passing Batch N.
- Zeta is required for Quark and was added explicitly after Packwiz did not pull it automatically.
- CraftTweaker, LootJS, Paxi, Item Obliterator, and Integrated Dungeons Arise remain delayed.
- Cataclysm addons, Ice and Fire addons, Marium addons, Create addon flood, RPG Series modules, Ars Nouveau, Theurgy, Dynamic Trees, Biomes O' Plenty, Sinytra Connector, Fabric-only mods, and additional structure/village packs beyond the current Batch N Integrated Villages/IDAS stack remain blocked until Jayden explicitly approves a later batch.

## Batch M Lighting And Lantern Risks

- Batch M is installed and verified. Active roots are Sodium Dynamic Lights, Amendments, Macaw's Lights and Lamps, and Decorative Blocks; Sodium Options API is the dynamic-light dependency.
- Do not install a second dynamic-light engine while Sodium Dynamic Lights is active. RyoamicLights and LambDynamicLights must stay out unless Sodium Dynamic Lights is removed first.
- Sodium Dynamic Lights and Sodium Options API are client-only and should not be copied to the dedicated server.
- Amendments is required with the current Supplementaries setup because Supplementaries moved wall lanterns, skull candles, ceiling pots, ceiling banners, and skull piles there.
- Amendments' custom jukebox disc model/spin is disabled in `config/amendments-client.toml` because its dynamic mask generation errors on Cataclysm's animated `music_disc_maledictus` texture. Keep the rest of Amendments active.
- Toni's Immersive Lanterns is delayed because Packwiz resolved Accessories to a NeoForge-named dependency in this Forge pack. Do not re-add it until the whole dependency chain is clean Forge 1.20.1.
- Decorative/block mods add world blocks. If Batch M is later removed, test worlds containing Macaw lights, Amendments blocks, or Decorative Blocks content can lose blocks.

## Main Menu And UI Polish Risks

- FancyMenu, Konkrete, Melody, WATERMeDIA, WATERMeDIA Binaries, and Immersive UI are client-only. They should not be copied to a dedicated server.
- FancyMenu video backgrounds now depend on WATERMeDIA and `wm_binaries-3.0.0-rc.1.jar`. This is a client-only release-candidate binary dependency. The old title-screen MP4 was found by WaterMedia but failed native playback as an audio-first High-profile MP4, then FancyMenu auto-cleared it after a watchdog timeout. The active packaged MP4 is re-encoded as video-only H.264 Baseline 1920x1080/30fps; keep that encoding unless a new in-game test proves another format works.
- The supplied title image is staged at `config/fancymenu/assets/ascendant_realms_title.png`, and Jayden's saved FancyMenu title-screen layout is packaged as the default `title_screen` layout.
- Do not hand-edit a complex FancyMenu layout unless there is already a known-good saved layout file from the active client.
- STONEBORN, Vanilla Experience+, FancyMenu, and Immersive UI can overlap visually. Treat menu ugliness as polish unless buttons become unreadable or unclickable.
- SpiffyHUD and Drippy Loading Screen extend FancyMenu and are client-only. If the client errors on startup, verify FancyMenu, Konkrete, and Melody before removing either addon.
- Item Borders, Stylish Effects, Overflowing Bars, and Health Bar Plus are client-only UI/readability layers. They can visually conflict with STONEBORN, Immersive UI, Legendary Tooltips, Xaero's Minimap, and shader/UI scaling, but they should not affect dedicated server logic.
- AppleSkin is intentionally both-side so multiplayer food, hunger, and saturation reads stay accurate for Farmer's Delight, Alex's Delight, and long survival tuning. The server materializer should copy AppleSkin but exclude the other UI tooling jars.
- RPG-Hud is delayed. Do not add it on top of SpiffyHUD until the current HUD stack is tested because it may replace or fight the active HUD/resource-pack layout.

## Skill Tree Upgrade Risks

- The custom skill tree is now one unified `Ascendant Web` instead of seven tabs. Existing test worlds may retain old player skill data; use a fresh test world or reset skill data for the cleanest validation.
- The web uses confirmed attribute reward types only. Boss-kill gates, Bountiful contract rewards, Create milestones, and title unlocks are documented future hooks, not active scripted behavior yet.
- The web gives 2 starting points, relies on default Puffish earned points, and adds managed milestone bonus points at levels 10, 20, 35, 50, 70, 90, and 110. Higher-tier nodes cost more, so players cannot max every branch quickly.
- XP uses killed mob dropped XP plus killed mob max health. Scaling Health and other enemy-scaling systems should naturally increase skill XP from stronger mobs, but balance still needs in-game tuning.
- Latest readability pass reduced the Ascendant Web from the older dense 343-link layout to 196 links. If future edits reintroduce heavy crossing-line clutter, regenerate and simplify before playtesting.
- The current visible level/nameplate system now uses `ar_skill_level`, which KubeJS mirrors from Puffish Skills Ascendant Web data. If levels do not show, inspect `kubejs/server_scripts/ascendant_progression.js`, `config/ascendant_progression/progression.json`, and the identity scoreboard functions before removing mods.
- The custom skill-XP HUD bar is live through KubeJS Painter and Jayden's 182x10 texture. The data bridge has been proven in-game; the active risk is visual polish and layering with the hotbar, vanilla XP bar, health/hunger/armor rows, Iron's Spells mana, chat, Xaero minimap, mob bars, Stylish Effects, and Overflowing Bars. If vanilla rows cannot be shifted enough by existing config/UI tooling, make a small client HUD helper mod rather than covering them with a KubeJS panel.
- KubeJS server scripts must not load `java.nio.file.Files`, `java.nio.file.Paths`, `java.io.File`, or `Java.from`, and repeated tick/death paths should avoid block-scoped loop locals that this KubeJS/Rhino build has already rejected. Use KubeJS `JsonIO` and the allowed `KubeJSPaths.CONFIG` handle for pack config reads, and keep repeated runtime loops conservative.
- KubeJS/Rhino also rejected resolving a Java `Path` object through `KubeJSPaths.CONFIG.resolve(...)` as ambiguous. Resolve pack config reads with the relative config string itself, such as `KubeJSPaths.CONFIG.resolve(String(relativePath))`, before calling `JsonIO`.
- Traveler's Titles visual packs must load after Vanilla Experience+ in root `options.txt`; otherwise broad font/resource overrides can flatten biome and dimension popups back to plain white Minecraft text.
- Enemy numeric levels are not currently shown. Scaling Health scales difficulty, but the inspected configs do not expose a clean `Level X` nameplate.
- The universal mod index and rarity schema are planning artifacts. They do not apply loot rarity, spawn tuning, or recipe changes until a later KubeJS/datapack/config pass implements those rules.
- The generated gear rarity registry is active documentation/config, but not every mod exposes exact weapon damage through data files. Rows with hardcoded Java stats or inherited attack damage use `Damage Status` notes and should be spot-checked in game before final balance. The generated tooltip script now shows only the rarity label; it intentionally hides scanned stat/index/reason text because native mod tooltips already show combat behavior.
- Item Borders now uses generated exact item-ID manual borders. If an item has no frame, first confirm the active CurseForge instance was reimported from the latest client export, then rerun `scripts/generate-gear-rarity-index.ps1` against the active instance.
- Item Borders config must stay UTF-8 without BOM. Forge/NightConfig reads a BOM as `ï»¿#` and crashes while loading `itemborders-common.toml`.
- The KubeJS rarity bridge is intentionally disabled/no-op for now. Some custom mod item classes, confirmed with Iron's Spells `InkItem`, do not expose a writable `item.rarity` field through KubeJS and can throw startup errors. Item Borders manual colors remain the authoritative rarity display.
- The generated client tooltip script uses `ItemEvents.tooltip` only. It must not use `ItemEvents.modification`, `event.modify`, or `item.rarity =`.
- The Guild/Hunter system is now installed and scaffolded, but not validated. Patchouli, FTB Quests, FTB Ranks, Easy NPC, CustomNPCs-Unofficial, and Human Companions need client/server boot testing before the pass is considered stable.
- FTB Quests and FTB Ranks are installed but not authored. Do not invent quest/rank config files until the in-game/editor-generated file formats are confirmed.
- Easy NPC, CustomNPCs-Unofficial, and Human Companions can overlap in NPC behavior and entity systems. If one causes crashes, isolate the specific tool instead of removing the entire Guild/Hunter stack.
- MCA Reborn is installed with MCA - Default Medieval enabled by default in root `options.txt`, and `scripts/check-pack.py` now enforces that pairing. It can overhaul villagers/families/social behavior and may still introduce off-tone skins or entity/server issues until the MCA-specific client/server test passes.
- NPC loadouts and nameplates are formal data contracts. The first generated CustomNPC spawn layer is now active, but it still uses static data-backed NPCs rather than a full runtime scheduler/equipment AI. A custom Forge helper mod is still likely for animated nameplates, dynamic levels, schedules, and automatic settlement role assignment.
- The Ascendant Settlements unification layer now has a standalone Guild/Hunter worldgen pilot. It must stay standalone until `hunter_board_village_standard`, `roadside_hunter_camp`, and `frontier_guild_outpost` pass `/locate`, client generation, dedicated server boot/join, and stability testing. Do not inject into vanilla or modded village pools until that gate passes.
- Guild Marks, Hunter Seals, Ascendant Sigils, rank functions, rival records, Hunter Board templates, nameplate profiles, NPC loadouts, and the Ascendant Codex are scaffolds. They need in-game testing, textures, NPC placement, and quest wiring before use on a real server world.
- Rival hunters should use nearby real NPCs plus off-screen simulated progress. Do not attempt always-loaded, world-spanning AI hunter simulation because it will create server performance and pathfinding risk.
- Root-cause policy is now active. Do not remove or quarantine mod content as the first response to a crash. Diagnose the exact failing config/data/resource/script/mod boundary, patch the smallest stable bridge, document it, and only delay/remove content when the specific feature is unfixable or unstable after a focused attempt.
- The current OpenLoader compatibility shim preserves content by overriding bad optional references: Human Companions keeps oak/spruce houses with a fixed biome typo, IDAS keeps Archmage/Enchanting Tower content with Iron's Spells loot/spawns instead of absent Ars Nouveau references, IceAndFire CE gets a shared `c:bosses` bridge, and optional recipe/advancement errors from missing compat mods are disabled with valid placeholders.
- Compatibility shims must be retested after every client import/server staging export. If a shim fixes startup but changes desired gameplay, adjust the shim rather than removing the owning mod.
- Ascendant Core is now the source-of-truth contract layer plus a first light runtime bridge. `config/ascendant_core/` and `kubejs/server_scripts/ascendant_core_integration.js` are live for registry checks, shared scoreboards, region tier mirroring, conservative kill reputation/proofs, and rank promotion. Region-aware spawns, formal rank-trial quests, final nameplates, NPC behavior, and settlement injection still need their specific bridges before they are considered finished gameplay.
- The Ascendant Core KubeJS loader must stay defensive. It may read JSON, check files, create objectives, mirror scoreboards, and award conservative counters; it must not rewrite worldgen, spawn rules, item rarity fields, or loot tables inside a server-load hook.
- Bountiful rejected `iceandfire:black_frost_dragon` as an objective target during runtime. The current active major Guild objective uses `iceandfire:ice_dragon` instead; only reintroduce Black Frost after confirming Bountiful accepts it in-game.
- The first unification implementation pass expands live Bountiful pools and lowers In Control caps for settlement safety. This preserves content, but it can change contract frequency and early-night pressure. Retest Bountiful boards, village survival, and server logs before calling the tuning final.
- Do not activate custom `spawner.json` rules directly in the main pack yet. In Control's custom spawner system has a separate schema from `spawn.json`, and bad additions can create invisible spawn or TPS problems.
- Resource-pack persistence root cause update: Vanilla Experience+ is now forced to download as ASCII `Vanilla Experience Plus.zip`. Do not restore the section-sign color-code filename; it can make Packwiz metadata, `options.txt`, and Resource Pack Overrides disagree and cause packs to drop back to defaults.
- Boot-speed cleanup is conservative. ModernFix dynamic resource/entity-renderer shortcuts and faster item rendering remain disabled because this pack has FancyMenu video, many resource packs, EMF/ETF, and large item tooltip/index layers. Only low-risk config tracking, reduced failed resource reload retries, lighter dynamic lights, lighter sound physics, and stale-file cleanup are active.

## Latest Ecosystem Pass Risks

- The newest imported CurseForge instance had no `latest.log` or crash report at audit time, so Minecraft-side confirmation still needs the next real client launch. Older-instance logs showed issues that are now patched in the repo.
- `scripts/sync-active-client-files.ps1` now targets the newest `Ascendant Realms*` instance and merges only resource-pack lines into `options.txt`. If a future full-options sync is needed, make it explicit so keybinds/audio/video settings are not overwritten accidentally.
- `scripts/sync-active-client-files.ps1` must only run while Minecraft is closed unless `-AllowWhileRunning` is intentionally used. It installs client-only helper jars like Resource Pack Overrides, preserves keybind/audio/video options, writes options without BOM, injects `version:3465`, and excludes Packwiz `.pw.toml` metadata from the live resourcepacks folder.
- `kubejs/client_scripts/ascendant_jei_aliases.js` keeps the Runic Grimoire path safe by avoiding the exact NBT Patchouli stack in `JEIEvents.addItems`; JEI rejected that stack as an empty ingredient. If a perfect named JEI entry is needed, add a small custom alias item later.
- `kubejs/client_scripts/ascendant_jei_aliases.js` keeps the Runic Grimoire JEI alias active but now passes the NBT stack directly. If the old `ingredients must not be empty` warning persists, isolate only the add-item alias before changing Patchouli or Simply Swords.
- The new Iron's Spells and IceAndFire armor texture aliases are visual-only resource fallbacks under `resourcepacks/ascendant-realms-compat-fixes/`. They do not change stats, loot, crafting, or server logic.

