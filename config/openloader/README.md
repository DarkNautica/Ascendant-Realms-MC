# Ascendant Realms OpenLoader Config

OpenLoader Forge 1.20.1 reads global datapacks from this config path:

```text
config/openloader/data/
```

The older repo-level `openloader/data/` folder is kept only as a source/legacy mirror. The active client and dedicated server tests must receive the files under `config/openloader/data/`.
