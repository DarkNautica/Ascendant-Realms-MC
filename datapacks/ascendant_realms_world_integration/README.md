# Ascendant Realms World Integration

This mirrors the OpenLoader global datapack in `config/openloader/data/ascendant_realms_world_integration`.

Open Loader is the active loading path for client and dedicated server tests. This copy exists so the world integration overrides are easy to inspect, copy into a save manually, or diff during tuning.

